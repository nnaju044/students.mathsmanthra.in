/**
 * utils/helpers.js — STUB
 *
 * Shared utility functions for the Maths Manthra application.
 * Add reusable helpers here as the project grows.
 *
 * Examples to implement:
 *   - formatDate(date) → localized date string
 *   - formatCurrency(amount) → ₹ formatted string
 *   - slugify(text) → URL-safe slug
 *   - paginate(data, page, limit) → pagination helper
 */
